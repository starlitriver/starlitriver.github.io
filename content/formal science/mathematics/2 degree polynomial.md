---
aliases:
  - quadratics
  - quadratic
  - 2 degree polynomials
tags: 
---

a 2 degree polynomial is a [[polynomial]] that has an maximum [[exponent]] of 2
it is also called a quadratic equation
the graph of a 2 degree polynomial is called a parabola

a 2 degree polynomial has a turning point - it is a point where the gradient is zero, and it is either the maximum or minimum point of the graph

2 degree polynomials can be represented in many forms, and some features of the graphs can be calculated using some expression you will need to memorize

## standard form
$f(x)=ax^2+bx+c$
where a b c are constants
the x coordinate of the vertex is $\dfrac{-b}{2a}$
find the x intercepts by substituting y as 0

## vertex form
$f(x)=a(x-h)^2+k$
where a h k are constants
(h,k) is the turning point of the equation
the y intercept is at  $(0,ah^2+k)$
find the x intercepts by substituting y as 0

## product form
$𝑓(𝑥) = 𝑎(𝑥 − 𝑥_1)(𝑥 − 𝑥_2)$
where a $x_1$ and $x_2$ are constants
the x-intercepts are are $(x_1,0)$ and $(x_2,0)$
