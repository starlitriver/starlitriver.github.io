---
aliases:
  - logarithms
tags: 
---

logarithms are a way of expressing "to what [[exponent|exponent]] do you raise the input to get the output"?

logarithms can be simply explained with this rule:

$\text{if }a^b=c\text{ then }\log_a(c)=b$$

for example, you have $2^4=16$ and $\sqrt[4]{16}=2$ and $\log_2(16)=4$
so, the log base 2 of 16 is 4

for example, you have $3^3=27$ and $\sqrt[3]{27}=3$ and $\log_3(27)=3$
so, the log base 3 of 27 is 3

when you aren't given a base, it is assumed that the base is 10.
for example, $\log(1000)$ is $\log_{10}(1000)$ which is 3


## solving

solving for logarithms operations with logarithms can be done with some rules that are similar to those of exponents


for reference, some operations on logarithms are quite similar to those of exponents

$\log_a(b)+\log_a(c)=\log_a(b\cdot c)$

$\log_a(b)-\log_a(c)=\log_a(\dfrac{b}{c})$

$\log_a(b^c)=c\cdot \log_a(b)$

$\log_a(b)=\dfrac{\log_{c}(b)}{\log_{c}(a)}$
